import { styled } from '@mui/material/styles';
import { ThemeBox } from '../../../common';

export const ProfileContainer = styled(ThemeBox)(() => ({
    margin: 'auto',
    padding: '2%',
    maxHeight: '100%',
    overflowY: 'auto',
    maxWidth: '750px'
}));
