import IAction from "../actions/IAction";

const initialState = {

}

export function sideBarReducer(state = initialState, action: IAction) {
    switch (state) {

        default:{
            return state;
        }
    }
}
